package main

import "core:fmt"

main :: proc() {
    fmt.println("Hellope!")
}

/* Above we're naming our package, then important a key library (?)
then using that library to print a string. */

//comment lines with double slashes.
/* also this 
for multiple lines */
